/*package cl.awakelab.certificacionpa.algorism;

import cl.awakelab.certificacionpa.entity.Producto;

import java.util.List;

public class CalculoSimple extends Descuento implements CalculoDescuento{

    @Override

    public static double calcularDescuento(Producto producto);
      if (cantidad > 20) {
            descuento = 0.3; // 30% de descuento
      } else if (cantidad >= 10 && cantidad <= 20) {
            descuento = 0.1; // 10% de descuento
      } else if (cantidad > 7) {
            descuento = 0.05; // 5% de descuento
      } else if (cantidad >= 5 && cantidad <= 7) {
            descuento = 0.03; // 3% de descuento
      }

    @Override
    public int calcular(List<Integer> numeros) {
        return 0;
    }


        return descuento;
    }
}

 */
